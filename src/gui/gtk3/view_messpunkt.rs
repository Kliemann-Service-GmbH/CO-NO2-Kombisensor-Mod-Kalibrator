use calib_error::CalibError;
use co_no2_kombisensor::sensor::{Sensor, SensorType};
use co_no2_kombisensor::kombisensor::{Kombisensor};
use std::sync::{Arc, Mutex};
use gtk;
use gtk::prelude::*;



pub fn launch(builder: &gtk::Builder) -> Result<(), CalibError> {

    Ok(())
}
